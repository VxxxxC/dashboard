credentials:
  usernames:
    admin:
      email: admin@gmail.com
      name: Admin
      logged_in: False
      # Default password is 'admin123'. CHANGE THIS before deploying to any shared environment.
      # Generate a new hash with: python3 -c "import bcrypt; print(bcrypt.hashpw(b'yourpassword', bcrypt.gensalt(12)).decode())"
      password: $2b$12$ESMh3SijxF.aHoHbvkMgQ.RHqVANwrH3hDwIFJaPpC5Arr7ZPL3C.
cookie:
  expiry_days: 30
  # IMPORTANT: Replace this key with a long random secret before deploying.
  # Generate one with: python3 -c "import secrets; print(secrets.token_hex(32))"
  key: 508f318e581d5323f2467c4d88bd437c6ea8ec6e41b925fc292f9e38bafdd1ff
  name: hummingbot_dashboard_auth
pre-authorized:
  emails:
  - admin@admin.com
